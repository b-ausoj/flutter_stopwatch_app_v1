import 'package:flutter/material.dart';

class ABG extends StatelessWidget {
  const ABG({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("todo agb"),
      ),
      body: const Center(
        child: Text("bli bla blop"),
      ),
    );
  }
}
